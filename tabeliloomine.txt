CREATE TABLE lugejad(
  email VARCHAR(30) NOT NULL PRIMARY KEY,
  laenutusi INT DEFAULT 0
);

INSERT INTO lugejad VALUES ('juku@eesti.ee', 14);

CREATE TABLE raamatud(
  id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  pealkiri VARCHAR(30),
  lugeja_email VARCHAR(30),
  FOREIGN KEY(lugeja_email) REFERENCES lugejad(email)
);

INSERT INTO raamatud VALUES (NULL, "Kevade", "juku@eesti.ee");
INSERT INTO raamatud VALUES (NULL, "Suvi", "juku@eesti.ee");

--

CREATE TABLE rentijad(
  email VARCHAR(30) NOT NULL PRIMARY KEY,
  rentimisi INT DEFAULT 0
);

INSERT INTO rentijad VALUES ('juku@eesti.ee', 14);

CREATE TABLE rendiautod(
  id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  autonumber VARCHAR(30),
  mark VARCHAR(30),
  mudel VARCHAR(30),
  rentija_email VARCHAR(30),
  FOREIGN KEY(rentija_email) REFERENCES rentijad(email)
);

INSERT INTO rendiautod VALUES (NULL, "313BMD", "Volvo", "S40", "juku@eesti.ee");
INSERT INTO rendiautod VALUES (NULL, "666LOL", "BMW", "X6", "juku@eesti.ee");
